<%= form_for(card) do |f| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cardnumber %>
    <%= f.number_field :cardnumber %>
  </div>

  <div class="field">
    <%= f.label :cardfront_url %>
    <%= f.text_field :cardfront_url %>
  </div>

  <div class="field">
    <%= f.label :cardback_url %>
    <%= f.text_field :cardback_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
